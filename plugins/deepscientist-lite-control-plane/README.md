# DS Lite Control Plane

Optional third-party control-plane extension for DS Lite 0.10 beta. Its bundled
`controller/` tree is the canonical runtime for DBOS-backed workflows. The
historical controller under `plugins/deepscientist-lite-core/controller` remains
only as a one-beta compatibility projection while downstream users migrate.
