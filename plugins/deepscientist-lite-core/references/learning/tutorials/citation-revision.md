# 引用与修订

逐条保留 reviewer concern，先分类严重性、行动和缺失输入，再起草回复。不得补造实验、引文、行号、图表或作者改动。每个 response 必须映射到稿件位置、证据、合理分歧或 `AUTHOR_INPUT_NEEDED`。引用验证区分 verified、conflict、not-found、pending；超时或 429 不能判成假引用。新增数值、新引文、删引文、越界路径和审批动作都要经过 revision constraint。结束前检查完整性、可追溯性、事实、语气和未解决风险。
