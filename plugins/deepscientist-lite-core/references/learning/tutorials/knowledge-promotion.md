# 知识晋升

Tapestry 负责捕获碎片，ScholarAIO 负责论文导入、解析和 collection；DS Lite 只消费带来源的 handoff。先去重、保留来源和不确定性，再生成 pending proposal。每个 claim 至少引用一个 source record；accepted 必须带目标知识库原生 review ref。撤回、冲突、superseded、更新和隐私边界都保留历史，不直接写正式知识，不把插件缓存当个人知识库。
