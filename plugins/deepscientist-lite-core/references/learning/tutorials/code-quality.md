# 代码质量

先把需求写成可追踪条目，再列允许路径、授权、指标和验收条件。低风险改动至少做 focused test；中风险加入单元测试、Given/When/Then 场景和变更行覆盖率；状态、权限、外部 I/O、迁移和发布代码还要做安全审查、失败注入、回退演练、定向变异和 fresh reviewer。工具没运行不算通过，覆盖率或 mutation 缺失不能写成“已验证”。审查真实实现、错误路径、凭据边界、日志脱敏和恢复性，不只看格式。
