# 公开网页取证

先声明允许域名、页数、字节、时间、调用、token 和费用预算。v1 只访问公开内容，不登录、不复用 cookie、不提交表单、不上传文件。每次重定向重新检查域名和私网地址。抓取结果必须保存媒体类型、获取时间、内容哈希、后端、转换链、相对 artifact 引用和失败层；配置了后端不等于实际使用。解析失败、拒绝、超时和内容变化都写 source record，不能用摘要代替原文。
