interface:
  display_name: "DS Lite Analysis Write"
  short_description: "Turn reviewed evidence into bounded claims, or route existing prose through the complete Nature polishing workflow."
  default_prompt: "$ds-lite-analysis-write summarize only the reviewed evidence, its limits, and the next step"
policy:
  allow_implicit_invocation: true
