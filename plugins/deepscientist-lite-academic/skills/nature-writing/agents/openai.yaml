interface:
  display_name: "Nature Writing"
  short_description: "Use the complete vendored nature-writing workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-writing for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
