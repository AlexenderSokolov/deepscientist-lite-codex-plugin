interface:
  display_name: "Nature Literature Pipeline"
  short_description: "Use the complete vendored nature-literature-pipeline workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-literature-pipeline for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
