interface:
  display_name: "Nature Response"
  short_description: "Use the complete vendored nature-response workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-response for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
