# Fixed case: missing experiment result

The author promises an ablation but supplies no output. Expected behavior:
keep the item `planned` or `needs-author-input`, require an Evidence Pack
reference before claiming completion, and keep the response package blocked.

