interface:
  display_name: "Nature Polishing"
  short_description: "Use the complete vendored nature-polishing workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-polishing for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
