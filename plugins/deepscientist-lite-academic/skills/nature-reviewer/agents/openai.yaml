interface:
  display_name: "Nature Reviewer"
  short_description: "Use the complete vendored nature-reviewer workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-reviewer for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
