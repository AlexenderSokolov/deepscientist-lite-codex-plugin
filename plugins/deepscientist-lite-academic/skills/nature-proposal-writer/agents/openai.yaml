interface:
  display_name: "Nature Proposal Writer"
  short_description: "Use the complete vendored nature-proposal-writer workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-proposal-writer for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
