interface:
  display_name: "Nature Downloader"
  short_description: "Use the complete vendored nature-downloader workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-downloader for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
