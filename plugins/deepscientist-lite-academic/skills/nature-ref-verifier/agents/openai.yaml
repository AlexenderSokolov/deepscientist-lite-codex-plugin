interface:
  display_name: "Nature Ref Verifier"
  short_description: "Use the complete vendored nature-ref-verifier workflow with DS Lite evidence and dependency boundaries."
  default_prompt: "Use $nature-ref-verifier for one bounded academic workflow."
policy:
  allow_implicit_invocation: true
