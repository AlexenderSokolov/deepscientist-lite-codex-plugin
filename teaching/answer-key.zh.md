# 教学参考答案

- 原始 fixture：完整性 pass、规范 pass、引用 not-applicable、方法对齐 pass，总决定 pass。
- finalize 后修改输出：完整性 fail，总决定 fail；重新写 review 文字不能修复哈希。
- accuracy 0.70 并重新 finalize：完整性可以 pass，但规范与指标 fail，总决定 fail。
- 无法联网核验的真实引用：citation 为 needs-human，不得推断 pass。
- 分支 C：即使主指标最高，读取受限标签构成规范 fail，必须 blocked。
- 分支 A 与 B：不存在脱离项目目标的唯一答案；必须依据契约中的预算和最终指标偏好选择。

核心解释：退出码只说明进程状态；阈值说明预先承诺的指标是否满足；review 还要检查完整性、规范、来源和方法对齐。
